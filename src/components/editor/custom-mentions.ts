import Mention from "@tiptap/extension-mention";

export const PageMention = Mention.extend({ name: "pageMention" });
export const ConversationMention = Mention.extend({ name: "conversationMention" });
