export const PAGE_SEMANTIC_WORKER_CONFIG = {
  BASE_DELAY_RETRY_MS: 10_000,
  MAX_TRANSIENT_BACKOFFS: 5,
  COOLDOWN_MS: 24 * 60 * 60 * 1000,
  STALE_AFTER_MS: 10 * 60 * 1000,
  MAX_JOBS_PER_TICK: 8,
} as const;
